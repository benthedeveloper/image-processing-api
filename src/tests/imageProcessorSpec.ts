import { imageProcessor } from '../image-processor.ts';

describe('imageProcessor tests', () => {
  describe('getFullImageAssetPath tests', () => {
    // TEST IDEAS:
    // 1. Passing in a filename that is not found should return null
    // 2. Passing in a filename that is found should return the path to the file
    // 3. Passing in a file with unsupported extension should return null
    it('filename that is not found should return null', async () => {
      expect(true).toBe(false);
    });

    it('returns true', async () => {
      expect(true).toBe(false);
    });

    it('returns true 2', async () => {
      expect(true).toBe(false);
    });
  });

  // describe('processImage tests', () => {
  //   it('returns true', () => {
  //     expect(true).toBe(false);
  //   });

  //   it('returns true', () => {
  //     expect(true).toBe(false);
  //   });

  //   it('returns true', () => {
  //     expect(true).toBe(false);
  //   });
  // });
});
